<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
    <title>{{ config.title | escape_xml | safe }}</title>
    <subtitle>{{ config.description | escape_xml | safe }}</subtitle>
    <link href="{{ feed_url | safe }}" rel="self" type="application/atom+xml"/>
    <link href="{{ config.base_url | safe }}"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>{{ last_updated | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</updated>
    <id>{{ config.base_url | safe }}</id>
    <author>
        <name>{{ config.extra.author | default(value="Madhava Jay") }}</name>
    </author>
    {%- for page in pages %}
    {%- if page.slug != "kalki" %}
    <entry xml:lang="{{ lang }}">
        <title>{{ page.title | escape_xml | safe }}</title>
        <published>{{ page.date | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</published>
        {%- if page.updated %}
        <updated>{{ page.updated | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</updated>
        {%- else %}
        <updated>{{ page.date | date(format="%Y-%m-%dT%H:%M:%S%:z") }}</updated>
        {%- endif %}
        <link href="{{ page.permalink | safe }}" type="text/html"/>
        <id>{{ page.permalink | safe }}</id>
        <content type="html">
            {%- if page.description %}
                {{ page.description | escape_xml | safe }}
            {%- elif page.summary %}
                {{ page.summary | escape_xml | safe }}
            {%- else %}
                {{ page.content | escape_xml | safe }}
            {%- endif %}
        </content>
    </entry>
    {%- endif %}
    {%- endfor %}
</feed>